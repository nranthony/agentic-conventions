# Beads prefix registry

One prefix per repo, 2–3 chars, registered here before `bd init -p <prefix>`.
Issues read `<prefix>-<hash>` (e.g. `zb-a3f2dd`).

| Prefix | Repo | Initialized | Notes |
|---|---|---|---|
| `zb` | project_zenbu | 2026-07-16 | Pilot repo (ADR-0005 in that repo) |

Reserved (not yet initialized):

| Prefix | Repo |
|---|---|
| `cv` | agentic-conventions |
